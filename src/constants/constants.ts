export const CurrenciesColors = {
  BTC: {
    chartColor: '#F7931A',
  },
};

export const MAX_DIGITS = 15;
export const MAX_DECIMAL_DIGITS: Record<'crypto' | 'fiat', number> = {
  crypto: 8,
  fiat: 2,
};
