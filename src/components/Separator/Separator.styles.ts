import {StyleSheet} from 'react-native';
import {colors} from '../../theme/colors';

export const separatorStyles = StyleSheet.create({
  line: {
    borderBottomWidth: 0.8,
    borderBottomColor: colors.onSurface[100],
  },
});
